using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stacker : MonoBehaviour
{
    private int randInt;

    // Start is called before the first frame update
    void Start()
    {
        randInt = Random.Range(-5, 5);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
